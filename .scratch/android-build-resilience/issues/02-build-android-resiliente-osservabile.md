> *This was generated by AI during triage.*

# 02 — Rendere il build Android resiliente e osservabile

**What to build:** Fornire un unico comando versionato che avvii il build Android in background, sopravviva all'interruzione della sessione chiamante e renda sempre visibili PID, fase corrente, log ed esito. I download esterni instabili devono usare retry e cache/mirror verificati, senza richiedere interventi manuali per i guasti transitori già osservati su FreeType e sulle dipendenze SDL.

**Blocked by:** 01 — Proteggere la cache del build Android.

**Status:** ready-for-agent

- [ ] Un solo comando avvia il build detached e restituisce immediatamente PID e percorso del log persistente.
- [ ] Un comando di stato distingue chiaramente `running`, `succeeded` e `failed`, mostrando fase corrente ed errore finale utile.
- [ ] L'interruzione o chiusura del terminale/agente chiamante non termina Buildozer o python-for-android.
- [ ] I download FreeType e i clone delle dipendenze SDL applicano retry, timeout ragionevoli e una cache o mirror affidabile con verifica dell'artefatto.
- [ ] Un fallimento lascia invariato il build number; un successo lo incrementa una sola volta e produce l'APK esatto atteso dalla versione.
- [ ] Il flusso riparte da una distribuzione parziale senza cancellare `.buildozer/` e completa build, verifica APK e installazione opzionale su dispositivo collegato.
- [ ] La skill `android-build` documenta e usa il nuovo flusso; Luna lo esegue end-to-end senza istruzioni aggiuntive.
